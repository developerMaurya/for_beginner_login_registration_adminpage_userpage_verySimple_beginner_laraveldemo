# create  migration and controler and mmodel 
php artisan make:model post -mcr
# note- 
-m, to create migration

-c to create controller

-r to specify the controller has resource

# migrate 
php artisan migrate